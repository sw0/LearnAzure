namespace LearnAzure.Common;


public static class Constants
{
    public const string AZURE_TENANT_ID = nameof(AZURE_TENANT_ID);
    public const string AZURE_CLIENT_ID = nameof(AZURE_CLIENT_ID);
    public const string AZURE_CLIENT_SECRET = nameof(AZURE_CLIENT_SECRET);
}
